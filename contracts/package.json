{
  "name": "sentinelx-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "SentinelX Algorand audit anchor contract",
  "scripts": {
    "compile": "python audit_contract.py",
    "deploy": "algokit project deploy testnet",
    "deploy:localnet": "algokit project deploy localnet"
  }
}
