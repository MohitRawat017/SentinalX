# Local development uses `.env.development`, which already points to localhost.
# For deployed frontend builds, VITE_API_URL must point to your FastAPI backend.
# Example:
# VITE_API_URL=https://your-backend-host.example.com
VITE_API_URL=
